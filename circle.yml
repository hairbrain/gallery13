machine:
  node:
    version: 0.12.2
dependencies:
  pre:
    - npm --version
    - git --version
    - npm install -g bower
    - npm i:
        pwd:
          client
  override:
    - bower i:
        pwd:
          client
